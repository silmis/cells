TODO:

Simulator
    outputs a Run object
Run
    encompasses the ruleset and the lifetime grid of the run

Ruleset:
    errors:
        make sure it works with large inputs
        make sure rulemap length is always states**nbh
        make sure rule length is always odd!

Generator:
    as subclass if ruleset?

Grid
    [no - other way around] uses char instead of int
